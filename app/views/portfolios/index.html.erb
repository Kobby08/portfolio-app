<%#  
The difference between prefix_path and prefix_url
prefix_path: gives the relative path of the url we are visiting. eg: (new_portfolio_path => /portfolios/new)
- Mostly used for in-app redirection.
prefix_url: gives the absolute path of the url we are visiting or want to redirect to. eg: (new_portfolio_url => localhost:3000/portfolios/new)
- Mostly used in emails to specify the full url to a page.
- Also accepts other args such as subdomains. eg: (portfolio_url, subdomain: "subdomain")
%>
<%= link_to "Create new Item", new_portfolio_path %>
<h1>Portfolio Items</h1>
<% @portfolio_items.each do |portfolio_item| %>
  <p><%= link_to portfolio_item.title, portfolio_path(portfolio_item) %></p>
  <p><%= portfolio_item.subtitle %></p>
  <p><%= portfolio_item.body %></p>
  <p><%= portfolio_item.main_image %></p>
  <p><%= image_tag portfolio_item.thumb_image unless portfolio_item.thumb_image.nil?%></p>
  <%= link_to "Edit", edit_portfolio_path(portfolio_item) %>
  <%= link_to "Delete Portfolio", portfolio_path(portfolio_item), method: :delete, data: {confirm: "Are you sure?"} %>
<% end %>
